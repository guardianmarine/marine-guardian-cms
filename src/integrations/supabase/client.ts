// src/integrations/supabase/client.ts
// DEPRECATED: Este archivo solo existe por compatibilidad retroactiva.
// Todos los nuevos imports deben usar: import { supabase } from '@/lib/supabaseClient';

import { supabase as supabaseClient } from '@/lib/supabaseClient';

export const supabase = supabaseClient;

